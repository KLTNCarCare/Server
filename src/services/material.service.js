const Material = require('../models/material.model');
const createMaterialService = async (material) =>{
    try {
        
    } catch (error) {
        
    }
};
module.exports = {
    createMaterialService,
    updateMaterialService,
    deleteMaterialService,
};